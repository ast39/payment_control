# Промпт для воссоздания проекта "Контроль платежей"

## 🎯 ОПИСАНИЕ ПРОЕКТА
Веб-приложение для контроля и управления платежами с календарным интерфейсом, системой уведомлений, управлением валютами, категориями и способами оплаты с полным CRUD функционалом. Включает расширенную статистику по валютам и продвинутую систему фильтрации платежей.

## 🏗️ ТЕХНИЧЕСКИЙ СТЕК

### Frontend:
- **React 18+** с функциональными компонентами и хуками
- **TypeScript** для типизации
- **Tailwind CSS** для стилизации
- **React Router** для навигации
- **Date-fns** для работы с датами
- **Lucide React** для иконок

### Backend:
- **Node.js** с Express.js
- **SQLite** как база данных
- **JWT** для аутентификации
- **bcrypt** для хеширования паролей
- **dotenv** для переменных окружения

## 📁 СТРУКТУРА ПРОЕКТА

```
payments/
├── backend/
│   ├── src/
│   │   ├── database/
│   │   │   ├── init.js          # Инициализация БД, схемы таблиц
│   │   │   └── migrate.js       # Миграции БД
│   │   ├── middleware/
│   │   │   └── auth.js          # JWT аутентификация
│   │   ├── routes/
│   │   │   ├── auth.js          # Логин/регистрация
│   │   │   ├── payments.js      # CRUD платежей с расширенной фильтрацией
│   │   │   ├── dashboard.js     # Календарь, статистика по валютам
│   │   │   ├── currencies.js    # Управление валютами
│   │   │   ├── categories.js    # Управление категориями
│   │   │   ├── payment-methods.js # Управление способами оплаты
│   │   │   └── settings.js      # Настройки уведомлений
│   │   └── index.js             # Основной сервер
│   ├── package.json
│   └── yarn.lock
├── src/
│   ├── components/
│   │   ├── auth/
│   │   │   └── Login.tsx        # Форма входа
│   │   ├── dashboard/
│   │   │   └── Dashboard.tsx    # Главная страница с календарем и статистикой по валютам
│   │   ├── payments/
│   │   │   ├── Payments.tsx     # Список платежей с расширенными фильтрами
│   │   │   ├── PaymentModal.tsx # Создание/редактирование
│   │   │   ├── PaymentDetails.tsx # Детали платежа
│   │   │   └── QuickPayModal.tsx # Быстрая оплата
│   │   ├── currencies/
│   │   │   └── Currencies.tsx   # Управление валютами
│   │   ├── categories/
│   │   │   └── Categories.tsx   # Управление категориями
│   │   ├── payment-methods/
│   │   │   └── PaymentMethods.tsx # Управление способами оплаты
│   │   ├── settings/
│   │   │   └── Settings.tsx     # Настройки уведомлений
│   │   └── layout/
│   │       └── Layout.tsx       # Общий layout с меню
│   ├── contexts/
│   │   └── AuthContext.tsx      # Контекст аутентификации
│   ├── App.tsx
│   └── index.tsx
├── Makefile                      # Команды для разработки
├── package.json
└── README.md
```

## 🗄️ СХЕМА БАЗЫ ДАННЫХ

### Таблица `users`:
```sql
CREATE TABLE users (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  username TEXT UNIQUE NOT NULL,
  password TEXT NOT NULL,
  name TEXT NOT NULL,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

### Таблица `currencies`:
```sql
CREATE TABLE currencies (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  name TEXT NOT NULL,           -- Название валюты (Российский рубль)
  code TEXT UNIQUE NOT NULL,    -- Код валюты (RUB, USD, EUR)
  symbol TEXT NOT NULL,         -- Символ валюты (₽, $, €)
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

### Таблица `payment_categories`:
```sql
CREATE TABLE payment_categories (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  name TEXT NOT NULL,           -- Название категории (Кредиты, Подписки)
  color TEXT DEFAULT '#6B7280', -- Цвет для отображения
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

### Таблица `payment_methods`:
```sql
CREATE TABLE payment_methods (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  name TEXT NOT NULL,           -- Название способа (Карта, Наличные, Перевод)
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

### Таблица `payments`:
```sql
CREATE TABLE payments (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  user_id INTEGER NOT NULL,
  title TEXT NOT NULL,
  description TEXT,
  amount DECIMAL(10,2) NOT NULL,
  currency_id INTEGER NOT NULL DEFAULT 1,      -- Ссылка на валюту
  category_id INTEGER NOT NULL DEFAULT 1,     -- Ссылка на категорию
  payment_method_id INTEGER NOT NULL DEFAULT 1, -- Ссылка на способ оплаты
  payment_date DATE,           -- Дата фактической оплаты (NULL если не оплачен)
  due_date DATE NOT NULL,      -- Дата когда должен быть оплачен
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (user_id) REFERENCES users(id),
  FOREIGN KEY (currency_id) REFERENCES currencies(id),
  FOREIGN KEY (category_id) REFERENCES payment_categories(id),
  FOREIGN KEY (payment_method_id) REFERENCES payment_methods(id)
);
```

### Таблица `user_settings`:
```sql
CREATE TABLE user_settings (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  user_id INTEGER UNIQUE NOT NULL,
  telegram_bot_token TEXT,
  telegram_chat_id TEXT,
  reminder_days INTEGER DEFAULT 3, -- За сколько дней уведомлять
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (user_id) REFERENCES users(id)
);
```

### Базовые данные:
- **Валюты**: RUB (₽), USD ($), EUR (€)
- **Категории**: Другое, Кредиты, Подписки
- **Способы оплаты**: Наличные, Карта, Перевод

## 🔐 СИСТЕМА АУТЕНТИФИКАЦИИ

### Регистрация/Вход:
- Форма с полями: логин, пароль, имя
- Валидация на фронте и бэке
- Хеширование паролей bcrypt
- JWT токены для сессий
- Защита всех роутов кроме `/auth`

### Middleware auth.js:
```javascript
const authenticateToken = (req, res, next) => {
  const token = req.headers.authorization?.split(' ')[1];
  if (!token) return res.status(401).json({ error: 'Токен не предоставлен' });
  
  jwt.verify(token, process.env.JWT_SECRET, (err, user) => {
    if (err) return res.status(403).json({ error: 'Недействительный токен' });
    req.user = user;
    next();
  });
};
```

## 💰 УПРАВЛЕНИЕ ВАЛЮТАМИ

### CRUD операции для валют:
- **CREATE** - создание новой валюты (название, код, символ)
- **READ** - список всех валют с сортировкой по названию
- **UPDATE** - редактирование существующей валюты
- **DELETE** - удаление валюты (только если не используется в платежах)

### API валют:
```
GET    /api/currencies          # Список всех валют
GET    /api/currencies/:id      # Получить конкретную валюту
POST   /api/currencies          # Создать новую валюту
PUT    /api/currencies/:id      # Обновить валюту
DELETE /api/currencies/:id      # Удалить валюту
```

## 🏷️ УПРАВЛЕНИЕ КАТЕГОРИЯМИ

### CRUD операции для категорий:
- **CREATE** - создание категории (название, цвет)
- **READ** - список категорий с цветовой индикацией
- **UPDATE** - редактирование названия и цвета
- **DELETE** - удаление категории (только если не используется в платежах)

### API категорий:
```
GET    /api/categories          # Список всех категорий
GET    /api/categories/:id      # Получить конкретную категорию
POST   /api/categories          # Создать новую категорию
PUT    /api/categories/:id      # Обновить категорию
DELETE /api/categories/:id      # Удалить категорию
```

## 💳 УПРАВЛЕНИЕ СПОСОБАМИ ОПЛАТЫ

### CRUD операции для способов оплаты:
- **CREATE** - создание способа оплаты (название)
- **READ** - список всех способов оплаты
- **UPDATE** - редактирование названия
- **DELETE** - удаление способа (только если не используется в платежах)

### API способов оплаты:
```
GET    /api/payment-methods          # Список всех способов
GET    /api/payment-methods/:id      # Получить конкретный способ
POST   /api/payment-methods          # Создать новый способ
PUT    /api/payment-methods/:id      # Обновить способ
DELETE /api/payment-methods/:id      # Удалить способ
```

## 📅 КАЛЕНДАРНЫЙ ИНТЕРФЕЙС И ДАШБОРД

### Логика цветов платежей:
- 🔵 **Синий** - запланированный (due_date в будущем, payment_date = NULL)
- 🟡 **Желтый** - скоро (due_date через ≤5 дней, payment_date = NULL)
- 🔴 **Красный** - просрочен (due_date < сегодня, payment_date = NULL)
- 🟢 **Зеленый** - оплачен (payment_date IS NOT NULL)

### Статистика по валютам:
- **Обновленная структура**: все суммы разбиты по валютам
- **Отдельные массивы** для total_amount, paid_amount, pending_amount, overdue_amount
- **Каждая валюта** содержит: id, name, code, symbol и соответствующие суммы
- **Компактный дизайн** карточек с иконкой, количеством платежей и суммами по валютам

### API календаря:
```
GET /api/dashboard/calendar/:year/:month
- Возвращает платежи за месяц с JOIN данными валют, категорий и способов оплаты
- Каждый платеж имеет поля: currency_symbol, category_name, category_color, payment_method_name
- Платежи сгруппированы по дням с полем `color` для отображения
```

### API статистики:
```
GET /api/dashboard/stats
- Возвращает статистику с разбивкой по валютам
- Структура ответа:
  stats: {
    total_payments: number,
    pending_payments: number,
    paid_payments: number,
    overdue_payments: number,
    total_amount: CurrencyAmount[],
    paid_amount: CurrencyAmount[],
    pending_amount: CurrencyAmount[],
    overdue_amount: CurrencyAmount[]
  }
```

### Интерактивность:
- **Клик по кнопке "+"** рядом с днем → создание платежа
- **Клик по платежу** → просмотр деталей с полной информацией
- **Навигация по месяцам** стрелками ← →

## 💰 УПРАВЛЕНИЕ ПЛАТЕЖАМИ

### Расширенная форма создания/редактирования:
- **Основные поля**: название, описание, сумма, дата платежа
- **Селектор валюты**: выбор из доступных валют с отображением символа
- **Селектор категории**: выбор категории с цветовой индикацией
- **Селектор способа оплаты**: выбор способа оплаты

### CRUD операции:
- **CREATE** - модалка создания с валидацией всех полей
- **READ** - список с расширенной фильтрацией и JOIN данными
- **UPDATE** - редактирование в модалке с сохранением всех связей
- **DELETE** - подтверждение удаления
- **Quick Pay** - быстрая отметка как оплаченный

### Расширенная фильтрация на странице платежей:
- **Форма фильтров** над таблицей в виде карточки
- **Период**: предыдущий, текущий, следующий месяц, все платежи
- **Валюта**: фильтр по выбранной валюте
- **Категория**: фильтр по выбранной категории
- **Способ оплаты**: фильтр по выбранному способу
- **Кнопка сброса** всех фильтров
- **Автоматическое обновление** при изменении любого фильтра
- **Комбинированная фильтрация** - все фильтры работают вместе

### Отображение:
- **Таблица с JOIN данными**: валюта, категория (с цветом), способ оплаты
- **Статусы платежей** с цветовой индикацией
- **Действия**: просмотр, редактирование, удаление, быстрая оплата

## 🔔 СИСТЕМА УВЕДОМЛЕНИЙ

### Настройки:
- Токен Telegram бота
- Chat ID для отправки
- Количество дней до уведомления

### Логика:
- Проверка просроченных платежей
- Отправка уведомлений в Telegram
- Настраиваемое время предупреждения

## 🎨 UI/UX ПРИНЦИПЫ

### Дизайн:
- **Современный минимализм** с Tailwind CSS
- **Адаптивная верстка** для всех устройств
- **Плавные анимации** и переходы
- **Интуитивная навигация**

### Компоненты:
- **Card-based layout** для блоков
- **Модальные окна** для форм
- **Hover эффекты** для интерактивности
- **Цветовая схема** для статусов платежей и категорий

### Обновленный дизайн дашборда:
- **Компактные карточки статистики** с иконками и крупными цифрами
- **Левая часть**: иконка сверху, количество платежей снизу (центрировано)
- **Правая часть**: заголовок карточки, суммы по валютам
- **Адаптивная сетка** для разных размеров экрана

### Иконки:
- **Coins** - универсальная иконка для валют
- **Tag** - для категорий
- **Wallet** - для способов оплаты
- **CreditCard** - для платежей

## 🚀 КОМАНДЫ ЗАПУСКА

### Makefile:
```makefile
dev:                    # Запуск в режиме разработки
	cd backend && yarn dev & cd .. && yarn start

backend:                # Только бэкенд
	cd backend && yarn dev

frontend:               # Только фронтенд
	yarn start

create-user:            # Создание нового пользователя
	cd backend && node scripts/createUser.js

reinit-db:              # Пересоздание БД с новой структурой
	cd backend && node database/init.js

migrate:                # Миграции БД
	cd backend && node database/migrate.js
```

## 📋 ПОРЯДОК РАЗРАБОТКИ

### 1. Настройка проекта:
```bash
mkdir payments && cd payments
# Backend
mkdir backend && cd backend
yarn init -y
yarn add express sqlite3 jsonwebtoken bcrypt dotenv cors
yarn add -D nodemon

# Frontend
cd ..
npx create-react-app . --template typescript
yarn add react-router-dom date-fns lucide-react
yarn add -D tailwindcss postcss autoprefixer
```

### 2. База данных:
- Создать `backend/src/database/init.js` с полной схемой
- Определить все таблицы: users, currencies, payment_categories, payment_methods, payments, user_settings
- Создать индексы для производительности
- Добавить базовые данные для валют, категорий и способов оплаты

### 3. Backend API:
- Настроить Express сервер
- Создать middleware аутентификации
- Реализовать все роуты:
  - auth (логин/регистрация)
  - payments (CRUD платежей с JOIN и расширенной фильтрацией)
  - currencies (CRUD валют)
  - categories (CRUD категорий)
  - payment-methods (CRUD способов оплаты)
  - dashboard (календарь и статистика по валютам)
  - settings (настройки уведомлений)

### 4. Frontend:
- Настроить React Router с новыми маршрутами
- Создать контекст аутентификации
- Реализовать все компоненты:
  - Currencies.tsx - управление валютами
  - Categories.tsx - управление категориями
  - PaymentMethods.tsx - управление способами оплаты
  - Обновить PaymentModal.tsx с новыми полями
  - Обновить Dashboard.tsx для отображения статистики по валютам
  - Обновить Payments.tsx с расширенными фильтрами

### 5. Интеграция:
- Подключить API к фронту
- Добавить обработку ошибок
- Реализовать валидацию форм
- Обновить отображение платежей с новой информацией
- Реализовать фильтрацию по всем параметрам

## 🧪 ТЕСТИРОВАНИЕ

### Backend:
- Проверить все API endpoints для новых сущностей
- Тестировать аутентификацию
- Проверить валидацию данных
- Тестировать JOIN запросы для платежей
- Проверить расширенную фильтрацию платежей
- Тестировать статистику по валютам

### Frontend:
- Тестировать навигацию по новым разделам
- Проверить формы создания/редактирования валют, категорий, способов оплаты
- Проверить обновленные формы платежей
- Тестировать интерактивность календаря с новой информацией
- Проверить работу фильтров на странице платежей
- Тестировать отображение статистики по валютам в дашборде

## 📱 ДЕПЛОЙ

### Требования:
- Node.js 18+
- SQLite3
- HTTPS для продакшена

### Переменные окружения:
```env
JWT_SECRET=your_jwt_secret_here
PORT=3001
NODE_ENV=development
```

## 🔧 ВОЗМОЖНЫЕ УЛУЧШЕНИЯ

- **PWA** - оффлайн режим
- **Push уведомления** в браузере
- **Экспорт данных** в Excel/PDF
- **Аналитика** и графики по валютам и категориям
- **Конвертация валют** в реальном времени
- **Мобильное приложение** на React Native
- **Мультиязычность**
- **Темная тема**
- **Шаблоны платежей** для быстрого создания
- **Группировка платежей** по периодам
- **Поиск по платежам** с автодополнением
- **Сортировка** по различным параметрам

## 📚 РЕСУРСЫ

- [React Documentation](https://react.dev/)
- [Tailwind CSS](https://tailwindcss.com/)
- [Express.js](https://expressjs.com/)
- [SQLite](https://www.sqlite.org/)
- [JWT](https://jwt.io/)
- [Date-fns](https://date-fns.org/)
- [Lucide Icons](https://lucide.dev/)
