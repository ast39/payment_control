# Payments Control - Приложение для контроля платежей

Современное веб-приложение для управления и отслеживания платежей с поддержкой валют, категорий и способов оплаты, красивым интерфейсом и функциональностью уведомлений. Включает расширенную статистику по валютам и продвинутую систему фильтрации платежей.

## 🚀 Возможности

- **Аутентификация пользователей** - безопасный вход в систему
- **Дашборд с календарем** - визуальное отображение платежей по дням
- **Расширенная статистика по валютам** - разбивка всех сумм по валютам в компактных карточках
- **Управление платежами** - создание, редактирование, удаление
- **Мультивалютность** - поддержка рублей, долларов, евро и других валют
- **Категоризация платежей** - группировка по типам (кредиты, подписки, другое)
- **Способы оплаты** - управление методами оплаты (карта, наличные, перевод)
- **Цветовая индикация** - разные цвета для разных статусов и категорий платежей
- **Расширенная фильтрация** - фильтры по периоду, валюте, категории и способу оплаты
- **Telegram уведомления** - настройка напоминаний о платежах
- **Адаптивный дизайн** - работает на всех устройствах

## 🛠 Технологии

### Frontend
- React 18 + TypeScript
- Tailwind CSS для стилизации
- React Router для навигации
- Lucide React для иконок
- Date-fns для работы с датами

### Backend
- Node.js + Express
- SQLite для базы данных
- JWT для аутентификации
- bcrypt для хеширования паролей

## 📦 Установка

### Предварительные требования
- Node.js 16+ 
- Yarn (рекомендуется) или npm

### 1. Клонирование и установка зависимостей
```bash
# Установка всех зависимостей
make install
```

### Структура проекта
```
payments/
├── backend/          # Node.js + Express API
│   ├── src/         # Исходный код
│   ├── package.json # Зависимости бэкенда
│   └── yarn.lock
├── frontend/        # React + TypeScript приложение
│   ├── src/         # Исходный код
│   ├── package.json # Зависимости фронтенда
│   └── yarn.lock
├── package.json     # Корневой package.json (монорепозиторий)
└── Makefile         # Команды для управления
```

### 2. Инициализация базы данных
База данных автоматически инициализируется при первом запуске с базовыми данными:
- **Валюты**: RUB (₽), USD ($), EUR (€)
- **Категории**: Другое, Кредиты, Подписки
- **Способы оплаты**: Наличные, Карта, Перевод

Если у вас уже есть существующая база данных, запустите миграцию:

```bash
make migrate
```

## 🚀 Запуск

### Запуск в разных терминалах

**Терминал 1 - Бэкенд:**
```bash
make start-backend
```

**Терминал 2 - Фронтенд:**
```bash
make start-frontend
```

### Или используйте команду для получения инструкций:
```bash
make start
```

## 🔐 Тестовый аккаунт

После установки автоматически создается тестовый пользователь:
- **Логин:** `admin`
- **Пароль:** `admin123`

## 📱 Использование

### 1. Вход в систему
- Откройте http://localhost:3000
- Войдите используя тестовый аккаунт

### 2. Дашборд
- **Статистика по валютам** - компактные карточки с разбивкой сумм по валютам
- **Календарь платежей** - навигация по месяцам с цветовой индикацией
- **Отслеживание** ближайших и просроченных платежей
- **Обновленный дизайн** - иконки, крупные цифры количества платежей, суммы по валютам

### 3. Управление валютами
- Создавайте новые валюты (название, код, символ)
- Редактируйте существующие валюты
- Удаляйте неиспользуемые валюты

### 4. Управление категориями
- Создавайте категории платежей с цветовой индикацией
- Настраивайте цвета для визуального различия
- Группируйте платежи по категориям

### 5. Управление способами оплаты
- Создавайте новые способы оплаты
- Редактируйте названия методов
- Удаляйте неиспользуемые способы

### 6. Управление платежами
- **Расширенная фильтрация** - форма фильтров над таблицей
- **Фильтры по периоду** - предыдущий/текущий/следующий месяц, все платежи
- **Фильтр по валюте** - выбор конкретной валюты
- **Фильтр по категории** - выбор категории платежей
- **Фильтр по способу оплаты** - выбор метода оплаты
- **Комбинированная фильтрация** - все фильтры работают вместе
- **Кнопка сброса** - быстрое очищение всех фильтров
- Создавайте новые платежи с выбором валюты, категории и способа оплаты
- Указывайте сумму в выбранной валюте
- Отмечайте выполненные платежи
- Просматривайте историю платежей

### 7. Настройки
- Настройте Telegram уведомления
- Укажите токен бота (получите у @BotFather)
- Укажите Chat ID (используйте @userinfobot)
- Настройте количество дней для напоминаний

## 🎨 Цветовая схема платежей

- **🔵 Синий** - запланированные платежи
- **🟡 Желтый** - менее 5 дней до оплаты
- **🔴 Красный** - просроченные платежи  
- **🟢 Зеленый** - оплаченные платежи

### Цвета категорий:
- Каждая категория имеет свой цвет для быстрой идентификации
- Цвета настраиваются при создании/редактировании категории

### Обновленный дизайн дашборда:
- **Компактные карточки статистики** с современным дизайном
- **Левая часть**: иконка сверху, количество платежей снизу (центрировано)
- **Правая часть**: заголовок карточки, суммы по валютам
- **Адаптивная сетка** для разных размеров экрана

## 📁 Структура проекта

```
payments/
├── backend/                 # Backend на Node.js
│   ├── src/
│   │   ├── routes/         # API маршруты
│   │   │   ├── auth.js     # Аутентификация
│   │   │   ├── payments.js # Платежи с расширенной фильтрацией
│   │   │   ├── currencies.js # Валюты
│   │   │   ├── categories.js # Категории
│   │   │   ├── payment-methods.js # Способы оплаты
│   │   │   ├── dashboard.js # Дашборд, календарь и статистика по валютам
│   │   │   └── settings.js # Настройки
│   │   ├── middleware/     # Middleware
│   │   ├── database/       # Инициализация БД
│   │   └── scripts/        # Скрипты
│   └── package.json
├── src/                    # Frontend на React
│   ├── components/         # React компоненты
│   │   ├── auth/          # Аутентификация
│   │   ├── dashboard/     # Дашборд с обновленной статистикой
│   │   ├── payments/      # Платежи с расширенными фильтрами
│   │   ├── currencies/    # Валюты
│   │   ├── categories/    # Категории
│   │   ├── payment-methods/ # Способы оплаты
│   │   ├── settings/      # Настройки
│   │   └── layout/        # Общий layout
│   ├── contexts/           # React контексты
│   └── index.tsx
├── public/                 # Статические файлы
├── Makefile               # Команды для запуска
└── README.md
```

## 🛠 Команды Makefile

```bash
make help              # Показать справку по командам
make install           # Установить все зависимости
make start-frontend    # Запустить фронтенд
make start-backend     # Запустить бэкенд
make start             # Показать инструкции по запуску
make create-user       # Создать нового пользователя
make migrate           # Запустить миграцию базы данных
make reinit-db         # Пересоздать БД с новой структурой
```

## 🔧 Создание пользователя

Для создания нового пользователя используйте:
```bash
make create-user
```

Эта команда запустит интерактивный скрипт, который попросит ввести:
- Имя пользователя
- Email
- Пароль

Или вручную:
```bash
cd backend
yarn run create-user
```

## 📊 API Endpoints

### Аутентификация
- `POST /api/auth/login` - вход в систему
- `POST /api/auth/register` - регистрация

### Платежи
- `GET /api/payments` - список платежей с JOIN данными и расширенной фильтрацией
- `POST /api/payments` - создание платежа
- `PUT /api/payments/:id` - обновление платежа
- `DELETE /api/payments/:id` - удаление платежа
- `POST /api/payments/:id/pay` - отметка как оплаченного

### Валюты
- `GET /api/currencies` - список валют
- `POST /api/currencies` - создание валюты
- `PUT /api/currencies/:id` - обновление валюты
- `DELETE /api/currencies/:id` - удаление валюты

### Категории
- `GET /api/categories` - список категорий
- `POST /api/categories` - создание категории
- `PUT /api/categories/:id` - обновление категории
- `DELETE /api/categories/:id` - удаление категории

### Способы оплаты
- `GET /api/payment-methods` - список способов оплаты
- `POST /api/payment-methods` - создание способа оплаты
- `PUT /api/payment-methods/:id` - обновление способа оплаты
- `DELETE /api/payment-methods/:id` - удаление способа оплаты

### Дашборд
- `GET /api/dashboard/calendar/:year/:month` - данные календаря с JOIN
- `GET /api/dashboard/stats` - статистика с разбивкой по валютам
- `GET /api/dashboard/day/:date` - платежи за день

### Настройки
- `GET /api/settings` - получить настройки
- `PUT /api/settings` - обновить настройки
- `POST /api/settings/test-telegram` - тест Telegram

## 🗄️ Структура базы данных

### Основные таблицы:
- **users** - пользователи системы
- **currencies** - валюты (RUB, USD, EUR)
- **payment_categories** - категории платежей
- **payment_methods** - способы оплаты
- **payments** - платежи с ссылками на валюты, категории и способы
- **user_settings** - настройки пользователей

### Связи:
- Каждый платеж связан с валютой, категорией и способом оплаты
- При удалении валюты/категории/способа проверяется использование в платежах
- Поддержка каскадных ограничений для целостности данных

## 🆕 Новые возможности

### Расширенная статистика по валютам:
- **Разбивка сумм по валютам** в дашборде
- **Компактные карточки** с иконками и крупными цифрами
- **Автоматическое обновление** при изменении данных
- **Адаптивный дизайн** для всех устройств

### Продвинутая фильтрация платежей:
- **Форма фильтров** над таблицей в виде карточки
- **Множественные фильтры** работают в комбинации
- **Автоматическое обновление** списка при изменении фильтров
- **Кнопка сброса** для быстрого очищения
- **Фильтры по**: периоду, валюте, категории, способу оплаты

## 🐛 Устранение неполадок

### Проблемы с базой данных
```bash
cd backend
yarn run init-db
```

### Проблемы с зависимостями
```bash
# Очистить кэш и переустановить
rm -rf node_modules yarn.lock
yarn install
```

### Проблемы с портами
По умолчанию:
- Frontend: http://localhost:3000
- Backend: http://localhost:3001

Измените порты в файлах конфигурации при необходимости.

## 🔄 Миграции

При обновлении структуры БД:
```bash
cd backend
node src/database/migrate.js
```

Для полного пересоздания БД:
```bash
cd backend
rm -f payments.db
node src/database/init.js
```

## 📝 Лицензия

Этот проект создан для демонстрации возможностей современной веб-разработки.

## 🤝 Поддержка

При возникновении проблем проверьте:
1. Версию Node.js (должна быть 16+)
2. Доступность портов 3000 и 3001
3. Логи в терминалах
4. Структуру базы данных

---

**Удачного использования! 🎉**
